[project]
name = "taichi-lang-ios-example"
version = "0.1.0"
description = "Taichi autodiff (forward/backward) AOT export sample for Metal"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "taichi==1.7.4",
]

[tool.uv]
package = false
